# Irada (إرادة)

A remote hiring platform empowering talents with disabilities
through accessible online jobs.

## Setup
1. Copy `.env.example` to `.env` and adjust values
2. Run `docker compose up -d`
3. Visit http://localhost:8069
4. Create a new database from the setup screen